server:
  port: 8070

eureka:
  instance:
    hostname: 47.129.55.176
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
